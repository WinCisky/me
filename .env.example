# Personal access token (classic or fine-grained) with no scopes: raises the GitHub API
# rate limit from 60 to 5000 requests/hour. Read server-side only, never exposed to the client.
# In production, set it as a project environment variable on Deno Deploy.
GITHUB_TOKEN=
